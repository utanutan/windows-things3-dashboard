# UIモックアップ仕様: 検索画面

**画面名**: Search View
**作成日**: 2026-02-01
**作成者**: Designer Agent

---

## 概要

検索画面は、すべてのタスクを横断的に検索する専用ビューです。タイトル、メモ、タグを対象にリアルタイム検索を行い、結果を即座に表示します。Things3の検索機能に相当します。

---

## レイアウト構造

### 検索中（結果あり）

```
┌───────────────────────────────────────────────────────────┐
│ Search                                                    │
│ (28px, bold)                                              │
├───────────────────────────────────────────────────────────┤
│                                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [🔍] 牛乳                               [×]         │ │
│ │      (検索ボックス - 拡大版)                        │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                           │
│ Found 3 results for "牛乳"                                │
│ (13px, secondary)                                         │
│                                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [ ] 牛乳を買う                            Today    │ │
│ │     📝 低脂肪乳                                    │ │
│ │     [買い物] [家事]                               │ │
│ │     📥 Inbox                                       │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ [ ] 牛乳パックを捨てる                    2/5     │ │
│ │     [家事]                                        │ │
│ │     📂 個人 > 家事プロジェクト                    │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ [✓] スーパーで牛乳購入                          │ │
│ │     📝 低脂肪1L                                    │ │
│ │     [買い物]                                      │ │
│ │     📥 Inbox                                       │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 検索前（空状態）

```
┌───────────────────────────────────────────────────────────┐
│ Search                                                    │
├───────────────────────────────────────────────────────────┤
│                                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [🔍] タスクを検索...                     [×]       │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                           │
│                                                           │
│             [MagnifyingGlassIcon (64px)]                  │
│                                                           │
│          Search across all tasks                          │
│      Find tasks by title, notes, or tags                  │
│                                                           │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 検索結果なし

```
┌───────────────────────────────────────────────────────────┐
│ Search                                                    │
├───────────────────────────────────────────────────────────┤
│                                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [🔍] xyz123                              [×]        │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                           │
│ No results found for "xyz123"                             │
│                                                           │
│                                                           │
│         [DocumentMagnifyingGlassIcon (64px)]              │
│                                                           │
│          No tasks match your search                       │
│      Try different keywords or check spelling             │
│                                                           │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## コンポーネント詳細

### 1. ヘッダーセクション

**レイアウト:**
```
┌────────────────────────────────────────────────┐
│ Search                                         │
└────────────────────────────────────────────────┘
```

**要素:**
- ビュータイトル: "Search"
- フォントサイズ: 28px
- フォントウェイト: 700
- カラー: text-primary
- マージン下: 24px

---

### 2. 検索ボックス（拡大版）

**レイアウト:**
```
┌─────────────────────────────────────────────────────┐
│ [🔍] タスクを検索...                     [×]       │
└─────────────────────────────────────────────────────┘
```

**要素:**

1. **検索アイコン** (左側)
   - アイコン: MagnifyingGlassIcon
   - サイズ: 20px
   - カラー: text-secondary
   - 位置: 左端から12px

2. **入力フィールド**
   - タイプ: text
   - プレースホルダー: "タスクを検索..."
   - フォントサイズ: 17px
   - パディング: 14px 48px（左右にアイコンスペース）
   - オートフォーカス: ページ表示時
   - デバウンス: 300ms

3. **クリアボタン** (右側)
   - アイコン: XMarkIcon
   - サイズ: 20px
   - 表示: 入力がある場合のみ
   - クリック: 検索ボックスをクリア

**スタイル:**
```css
.search-box-large {
  position: relative;
  width: 100%;
  margin-bottom: 24px;
}

.search-input-large {
  width: 100%;
  padding: 14px 48px;
  border: 2px solid var(--color-border-medium);
  border-radius: 12px;
  font-size: 17px;
  color: var(--color-text-primary);
  background-color: var(--color-background-main);
  transition: all 0.15s ease;
}

.search-input-large:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 4px var(--color-primary-light);
}

.search-input-large::placeholder {
  color: var(--color-text-tertiary);
}

.search-icon {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  width: 20px;
  height: 20px;
  color: var(--color-text-secondary);
  pointer-events: none;
}

.search-clear-button {
  position: absolute;
  right: 8px;
  top: 50%;
  transform: translateY(-50%);
  width: 32px;
  height: 32px;
  border-radius: 6px;
  background-color: transparent;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.15s ease;
  color: var(--color-text-secondary);
}

.search-clear-button:hover {
  background-color: var(--color-background-hover);
  color: var(--color-text-primary);
}
```

---

### 3. 検索結果ヘッダー

**レイアウト:**
```
┌────────────────────────────────────────────────┐
│ Found 3 results for "牛乳"                     │
└────────────────────────────────────────────────┘
```

**要素:**
- テキスト: "Found {count} results for \"{query}\""
- 件数が0の場合: "No results found for \"{query}\""
- フォントサイズ: 13px
- カラー: text-secondary
- マージン下: 16px

**スタイル:**
```css
.search-result-header {
  font-size: 13px;
  color: var(--color-text-secondary);
  margin-bottom: 16px;
}

.search-query-highlight {
  font-weight: 600;
  color: var(--color-text-primary);
}
```

---

### 4. 検索結果リスト

**タスクアイテム構造:**
```
┌─────────────────────────────────────────────────────┐
│ [ ] 牛乳を買う                            Today    │
│     📝 低脂肪乳                                    │
│     [買い物] [家事]                               │
│     📥 Inbox                                       │
└─────────────────────────────────────────────────────┘
```

**標準のタスクアイテムに追加:**
1. **所属先表示** (新規要素)
   - 位置: タグの下
   - フォントサイズ: 12px
   - カラー: text-tertiary
   - アイコン:
     - Inbox: InboxIcon (14px)
     - Today: CalendarDaysIcon (14px)
     - Area: FolderIcon (14px)
     - Project: RectangleStackIcon (14px)
   - 形式: "📥 Inbox" または "📂 仕事 > プロジェクトA"

2. **ハイライト表示** (検索キーワード)
   - 検索クエリに一致する部分をハイライト
   - 背景: primary-light (#E8F4FD)
   - フォントウェイト: 600

**スタイル:**
```css
.search-result-item {
  /* 標準のタスクアイテムスタイル */
}

.search-result-location {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-top: 8px;
  margin-left: 32px;
  font-size: 12px;
  color: var(--color-text-tertiary);
}

.search-result-location-icon {
  width: 14px;
  height: 14px;
  flex-shrink: 0;
}

.search-highlight {
  background-color: var(--color-primary-light);
  font-weight: 600;
  padding: 0 2px;
  border-radius: 2px;
}
```

---

### 5. 空状態（検索前）

**レイアウト:**
```
┌────────────────────────────────────────────────┐
│                                                │
│      [MagnifyingGlassIcon (64px)]              │
│                                                │
│       Search across all tasks                  │
│   Find tasks by title, notes, or tags         │
│                                                │
└────────────────────────────────────────────────┘
```

**要素:**

1. **アイコン**
   - アイコン: MagnifyingGlassIcon
   - サイズ: 64x64px
   - カラー: text-tertiary
   - マージン下: 16px

2. **タイトル**
   - テキスト: "Search across all tasks"
   - フォントサイズ: 17px
   - フォントウェイト: 600
   - カラー: text-primary
   - マージン下: 8px

3. **説明文**
   - テキスト: "Find tasks by title, notes, or tags"
   - フォントサイズ: 15px
   - カラー: text-secondary

**スタイル:**
```css
.search-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 400px;
  text-align: center;
}

.search-empty-icon {
  width: 64px;
  height: 64px;
  color: var(--color-text-tertiary);
  margin-bottom: 16px;
}

.search-empty-title {
  font-size: 17px;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 8px;
}

.search-empty-description {
  font-size: 15px;
  color: var(--color-text-secondary);
}
```

---

### 6. 空状態（検索結果なし）

**レイアウト:**
```
┌────────────────────────────────────────────────┐
│ No results found for "xyz123"                  │
│                                                │
│   [DocumentMagnifyingGlassIcon (64px)]         │
│                                                │
│       No tasks match your search               │
│   Try different keywords or check spelling     │
│                                                │
└────────────────────────────────────────────────┘
```

**要素:**

1. **ヘッダーメッセージ**
   - テキスト: "No results found for \"{query}\""
   - フォントサイズ: 15px
   - カラー: text-secondary
   - マージン下: 24px

2. **アイコン**
   - アイコン: DocumentMagnifyingGlassIcon
   - サイズ: 64x64px
   - カラー: text-tertiary
   - マージン下: 16px

3. **タイトル**
   - テキスト: "No tasks match your search"
   - フォントサイズ: 17px
   - フォントウェイト: 600
   - カラー: text-primary
   - マージン下: 8px

4. **ヒント**
   - テキスト: "Try different keywords or check spelling"
   - フォントサイズ: 15px
   - カラー: text-secondary

**スタイル:**
```css
.search-no-results {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 400px;
  text-align: center;
}

.search-no-results-header {
  font-size: 15px;
  color: var(--color-text-secondary);
  margin-bottom: 24px;
}

.search-no-results-icon {
  width: 64px;
  height: 64px;
  color: var(--color-text-tertiary);
  margin-bottom: 16px;
}

.search-no-results-title {
  font-size: 17px;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 8px;
}

.search-no-results-hint {
  font-size: 15px;
  color: var(--color-text-secondary);
}
```

---

## インタラクション仕様

### 検索実行

**デバウンス処理:**
- 入力後300ms待機してから検索実行
- ユーザーが入力を続けている間は待機
- 入力が停止してから300ms後にAPI呼び出し

**フロー:**
1. ユーザーが検索ボックスに入力
2. 300msデバウンス待機
3. ローディング状態表示（検索ボックスにスピナー）
4. API呼び出し (`GET /todos/search?q={query}`)
5. 結果を表示

**疑似コード:**
```typescript
const [query, setQuery] = useState('');
const [results, setResults] = useState<Task[]>([]);
const [isLoading, setIsLoading] = useState(false);

const debouncedSearch = useMemo(
  () =>
    debounce(async (searchQuery: string) => {
      if (!searchQuery.trim()) {
        setResults([]);
        return;
      }

      setIsLoading(true);
      try {
        const response = await fetch(
          `/todos/search?q=${encodeURIComponent(searchQuery)}`
        );
        const data = await response.json();
        setResults(data);
      } catch (error) {
        console.error('Search failed:', error);
      } finally {
        setIsLoading(false);
      }
    }, 300),
  []
);

useEffect(() => {
  debouncedSearch(query);
}, [query, debouncedSearch]);
```

### ハイライト表示

**クライアント側実装:**
```typescript
const highlightText = (text: string, query: string): JSX.Element => {
  if (!query.trim()) return <>{text}</>;

  const regex = new RegExp(`(${escapeRegex(query)})`, 'gi');
  const parts = text.split(regex);

  return (
    <>
      {parts.map((part, i) =>
        regex.test(part) ? (
          <span key={i} className="search-highlight">
            {part}
          </span>
        ) : (
          <span key={i}>{part}</span>
        )
      )}
    </>
  );
};
```

### クリアボタン

**クリック:**
- 検索ボックスをクリア
- 検索結果をリセット
- 検索ボックスにフォーカス維持

### タスクアイテムクリック

**クリック:**
- タスク詳細パネルを開く
- 検索結果ビューは維持（戻る時に検索結果に戻る）

**所属先クリック:**
- 該当のビュー（Inbox、エリア、プロジェクト）に遷移
- 検索履歴に追加（ブラウザバック対応）

---

## データ連携

### API エンドポイント

**検索:**
```
GET /todos/search?q={query}
Response: Task[]
```

**クエリパラメータ:**
- `q`: 検索クエリ（URLエンコード済み）

**レスポンス例:**
```json
[
  {
    "id": "task-1",
    "title": "牛乳を買う",
    "notes": "低脂肪乳",
    "due_date": "2026-02-01",
    "tags": ["買い物", "家事"],
    "status": "open",
    "list": "inbox",
    "area_id": null,
    "project_id": null
  },
  {
    "id": "task-2",
    "title": "牛乳パックを捨てる",
    "notes": null,
    "due_date": "2026-02-05",
    "tags": ["家事"],
    "status": "open",
    "list": null,
    "area_id": "area-2",
    "project_id": "proj-5"
  }
]
```

### 検索対象

**サーバー側（推奨）:**
- タスクタイトル（大文字小文字を区別しない）
- タスクメモ
- タグ

**クライアント側フィルタリング（オプション）:**
```typescript
const filterTasks = (tasks: Task[], query: string): Task[] => {
  const lowerQuery = query.toLowerCase();
  return tasks.filter(
    (task) =>
      task.title.toLowerCase().includes(lowerQuery) ||
      task.notes?.toLowerCase().includes(lowerQuery) ||
      task.tags?.some((tag) => tag.toLowerCase().includes(lowerQuery))
  );
};
```

---

## ローディング状態

### 検索中

**検索ボックス:**
```
┌─────────────────────────────────────────────────────┐
│ [⟳] 牛乳                                [×]        │
└─────────────────────────────────────────────────────┘
```

**スタイル:**
```css
.search-icon.loading {
  animation: spin 0.6s linear infinite;
}

@keyframes spin {
  to { transform: translateY(-50%) rotate(360deg); }
}
```

### スケルトンローダー

```
Found ... results for "..."

┌─────────────────────────────────────────────────────┐
│ ▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯    ▯▯▯▯        │
│     ▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯                             │
│     ▯▯▯▯▯ ▯▯▯▯▯                                  │
│     ▯▯▯▯▯▯▯▯▯▯▯▯                                │
├─────────────────────────────────────────────────────┤
│ ▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯    ▯▯▯▯        │
└─────────────────────────────────────────────────────┘
```

---

## レスポンシブ対応

### デスクトップ (≥ 1024px)
- 検索ボックス: フル幅
- パディング: 32px

### タブレット (768px - 1023px)
- 検索ボックス: フル幅
- パディング: 24px

### モバイル (< 768px)
- 検索ボックス: フル幅、やや縮小
- パディング: 16px
- フォントサイズ: 15px（検索ボックス）

---

## アクセシビリティ

### ARIAラベル

```html
<!-- 検索ボックス -->
<div role="search">
  <label for="search-input" class="sr-only">Search tasks</label>
  <input
    id="search-input"
    type="search"
    aria-label="Search tasks by title, notes, or tags"
    aria-describedby="search-instructions"
    aria-autocomplete="list"
  />
  <span id="search-instructions" class="sr-only">
    Type to search across all tasks. Results will update automatically.
  </span>
</div>

<!-- 検索結果 -->
<div role="region" aria-live="polite" aria-atomic="true">
  <p class="search-result-header">
    Found <span aria-label="3 results">3</span> results for "牛乳"
  </p>
  <div role="list" aria-label="Search results">
    <!-- タスクアイテム -->
  </div>
</div>
```

### キーボードナビゲーション

- `Tab`: 次のタスクにフォーカス
- `Shift + Tab`: 前のタスクにフォーカス
- `↑` / `↓`: タスク間を移動（オプション）
- `Enter`: タスク詳細を開く
- `Esc`: 検索ボックスをクリア

---

## 実装ノート

1. **デバウンス**: `lodash.debounce` または `use-debounce` フック使用
2. **URLパラメータ**: 検索クエリをURLに反映 (`/search?q=...`)
3. **ブラウザバック**: 検索クエリとスクロール位置を保持
4. **パフォーマンス**: 検索結果が多い場合は仮想スクロール使用
5. **キャッシュ**: 検索結果をキャッシュして再検索時に高速化

**URL統合例:**
```typescript
const router = useRouter();

useEffect(() => {
  if (query) {
    router.push(`/search?q=${encodeURIComponent(query)}`, undefined, {
      shallow: true
    });
  } else {
    router.push('/search', undefined, { shallow: true });
  }
}, [query]);
```

---

**関連ドキュメント:**
- `docs/design_system.md` - デザインシステム全体
- `resources/mockups/dashboard.md` - ダッシュボード全体構造
- `resources/mockups/inbox.md` - Inbox画面
- `resources/mockups/task_create_form.md` - タスク作成フォーム
