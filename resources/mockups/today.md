# UIモックアップ仕様: Today画面

**画面名**: Today View
**作成日**: 2026-02-01
**作成者**: Designer Agent

---

## 概要

Today画面は、本日予定されているタスクを表示する専用ビューです。Things3の"Today"リストに相当し、その日に集中すべきタスクを明確に表示します。期限が今日のタスクと、手動で"Today"に追加されたタスクが表示されます。

---

## レイアウト構造

```
┌───────────────────────────────────────────────────────────┐
│ Today                               [+ New Task]          │
│ Saturday, February 1                (プライマリボタン)    │
├───────────────────────────────────────────────────────────┤
│                                                           │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ OVERDUE (期限切れ) - 2 tasks                             │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [ ] プロジェクト提案書作成                  1/30  │ │
│ │     📝 期限を過ぎています                         │ │
│ │     [仕事] [重要]                                 │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ [ ] 確定申告書類準備                       1/29  │ │
│ │     [個人]                                        │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                           │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ TODAY - 3 tasks                                           │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [ ] 牛乳を買う                              Today │ │
│ │     📝 低脂肪乳                                    │ │
│ │     [買い物] [家事]                               │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ [ ] 週次レポート作成                        Today │ │
│ │     [仕事]                                        │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ [✓] ミーティング資料準備                   Today │ │
│ │     📝 スライド作成完了                          │ │
│ │     [仕事]                                        │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                           │
│ 5 tasks (2 overdue, 3 today)                              │
└───────────────────────────────────────────────────────────┘
```

---

## コンポーネント詳細

### 1. ヘッダーセクション

**レイアウト:**
```
┌────────────────────────────────────────────────┐
│ Today                          [+ New Task]    │
│ Saturday, February 1                           │
└────────────────────────────────────────────────┘
```

**要素:**

1. **ビュータイトル** (左上)
   - テキスト: "Today"
   - フォントサイズ: 28px
   - フォントウェイト: 700
   - カラー: text-primary (#1C1C1E)

2. **日付** (左下)
   - テキスト: "Saturday, February 1" (曜日, 月 日)
   - フォントサイズ: 15px
   - フォントウェイト: 400
   - カラー: text-secondary (#6E6E73)
   - マージン上: 4px

3. **New Taskボタン** (右上)
   - スタイル: プライマリボタン
   - テキスト: "+ New Task"
   - クリック時: タスク作成モーダルを開き、`list` を "today" に設定

**スタイル:**
```css
.today-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 32px;
}

.today-header-left {
  display: flex;
  flex-direction: column;
}

.today-title {
  font-size: 28px;
  font-weight: 700;
  color: var(--color-text-primary);
}

.today-date {
  font-size: 15px;
  font-weight: 400;
  color: var(--color-text-secondary);
  margin-top: 4px;
}
```

---

### 2. セクションヘッダー（Overdue/Today）

**構造:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OVERDUE - 2 tasks
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**要素:**

1. **上部ボーダー**
   - ボーダー: 1px solid divider (#EBEBEB)
   - マージン下: 12px

2. **セクションタイトル**
   - テキスト: "OVERDUE - 2 tasks" または "TODAY - 3 tasks"
   - フォントサイズ: 13px
   - フォントウェイト: 600
   - カラー:
     - OVERDUE: error (#FF3B30)
     - TODAY: text-secondary (#6E6E73)
   - レタースペーシング: 0.5px
   - テキスト変換: uppercase
   - パディング: 8px 0

3. **下部ボーダー**
   - ボーダー: 1px solid divider (#EBEBEB)
   - マージン下: 16px

**スタイル:**
```css
.section-header {
  margin: 24px 0 16px 0;
}

.section-header-border {
  border-top: 1px solid var(--color-divider);
  margin-bottom: 12px;
}

.section-header-title {
  font-size: 13px;
  font-weight: 600;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  padding: 8px 0;
}

.section-header-title.overdue {
  color: var(--color-error);
}

.section-header-title.today {
  color: var(--color-text-secondary);
}

.section-header-border-bottom {
  border-bottom: 1px solid var(--color-divider);
  margin-bottom: 16px;
}
```

---

### 3. Overdue セクション

**目的:**
期限切れのタスクを目立たせて表示し、優先度を明確にする

**タスクアイテム - Overdue:**
```
┌─────────────────────────────────────────────────────┐
│ [ ] プロジェクト提案書作成                  1/30  │
│     📝 期限を過ぎています                         │
│     [仕事] [重要]                                 │
└─────────────────────────────────────────────────────┘
```

**特徴:**
1. **期限表示**
   - カラー: error (#FF3B30)
   - 背景: rgba(255, 59, 48, 0.1)
   - フォントウェイト: 600
   - アイコン: ExclamationCircleIcon (14px) ※オプション

2. **強調表示**
   - 左ボーダー: 3px solid error (#FF3B30) ※オプション
   - 背景: 微妙な赤味 rgba(255, 59, 48, 0.02) ※オプション

**スタイル:**
```css
.task-item.overdue {
  border-left: 3px solid var(--color-error);
  background-color: rgba(255, 59, 48, 0.02);
}

.task-item.overdue .task-due-date {
  color: var(--color-error);
  background-color: rgba(255, 59, 48, 0.1);
  font-weight: 600;
}
```

---

### 4. Today セクション

**目的:**
本日予定のタスクを表示

**タスクアイテム - Today:**
```
┌─────────────────────────────────────────────────────┐
│ [ ] 牛乳を買う                              Today │
│     📝 低脂肪乳                                    │
│     [買い物] [家事]                               │
└─────────────────────────────────────────────────────┘
```

**特徴:**
1. **期限表示**
   - テキスト: "Today"
   - カラー: warning (#FF9500)
   - 背景: rgba(255, 149, 0, 0.1)
   - フォントウェイト: 500

2. **通常表示**
   - Overdue のような強調表示なし
   - 標準のタスクアイテムスタイル

**スタイル:**
```css
.task-item.today .task-due-date {
  color: var(--color-warning);
  background-color: rgba(255, 149, 0, 0.1);
  font-weight: 500;
}
```

---

### 5. フッターセクション

**レイアウト:**
```
┌────────────────────────────────────────────────┐
│ 5 tasks (2 overdue, 3 today)                   │
└────────────────────────────────────────────────┘
```

**要素:**
- タスク件数サマリー
- フォントサイズ: 11px
- カラー: text-tertiary (#AEAEB2)
- パディング上: 16px
- 中央寄せ

**スタイル:**
```css
.today-footer {
  padding-top: 16px;
  text-align: center;
  font-size: 11px;
  color: var(--color-text-tertiary);
}
```

---

### 6. 空状態

#### Overdue なし（Today タスクあり）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TODAY - 3 tasks
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(タスクリスト)
```

#### 完全に空（Overdue も Today もなし）

```
┌────────────────────────────────────────────────┐
│                                                │
│          [CalendarDaysIcon (64px)]             │
│                                                │
│          Nothing planned for today             │
│       Enjoy your day or add a new task         │
│                                                │
│            [+ New Task]                        │
│                                                │
└────────────────────────────────────────────────┘
```

**スタイル:**
```css
.today-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 400px;
  text-align: center;
}

.today-empty-icon {
  width: 64px;
  height: 64px;
  color: var(--color-text-tertiary);
  margin-bottom: 16px;
}

.today-empty-title {
  font-size: 17px;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 8px;
}

.today-empty-description {
  font-size: 15px;
  color: var(--color-text-secondary);
  margin-bottom: 24px;
}
```

---

## インタラクション仕様

### セクション折りたたみ（オプション機能）

**動作:**
- セクションヘッダーをクリックして展開/折りたたみ
- Chevronアイコン (ChevronDownIcon / ChevronUpIcon) を表示
- アニメーション: 300ms ease

**状態:**
- デフォルト: 両セクションとも展開
- 折りたたみ時: ヘッダーのみ表示、タスク数は維持

**スタイル:**
```css
.section-header-clickable {
  cursor: pointer;
  user-select: none;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.section-header-chevron {
  width: 16px;
  height: 16px;
  transition: transform 0.3s ease;
}

.section-header-chevron.collapsed {
  transform: rotate(-90deg);
}

.section-content {
  overflow: hidden;
  transition: max-height 0.3s ease;
}

.section-content.collapsed {
  max-height: 0;
}
```

### タスクアイテム

**ホバー/クリック/チェックボックス:**
- Inbox画面と同様の動作
- 詳細は `resources/mockups/inbox.md` を参照

### Overdue タスクの移動

**コンテキストメニュー（オプション）:**
- 右クリック: "Move to Today" オプション
- クリック時: 期限を今日に変更
- 成功時: タスクがTodayセクションに移動

---

## データ連携

### API エンドポイント

**Today タスク一覧取得:**
```
GET /todos/today
Response: Task[]
```

**レスポンス例:**
```json
[
  {
    "id": "task-1",
    "title": "プロジェクト提案書作成",
    "notes": "期限を過ぎています",
    "due_date": "2026-01-30",
    "tags": ["仕事", "重要"],
    "status": "open"
  },
  {
    "id": "task-2",
    "title": "牛乳を買う",
    "notes": "低脂肪乳",
    "due_date": "2026-02-01",
    "tags": ["買い物", "家事"],
    "status": "open"
  }
]
```

### クライアント側ロジック

**タスクのセクション分け:**
```typescript
// 疑似コード
const today = new Date().toISOString().split('T')[0]; // "2026-02-01"

const overdueTasks = tasks.filter(task =>
  task.due_date && task.due_date < today && task.status === 'open'
);

const todayTasks = tasks.filter(task =>
  task.due_date === today && task.status === 'open'
);

const completedTasks = tasks.filter(task =>
  task.status === 'completed'
);
```

**タスク件数計算:**
```typescript
const totalTasks = overdueTasks.length + todayTasks.length;
const summary = `${totalTasks} tasks (${overdueTasks.length} overdue, ${todayTasks.length} today)`;
```

---

## ローディング状態

### スケルトンローダー（セクション別）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────┐
│ ▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯    ▯▯▯▯        │
│     ▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯                             │
├─────────────────────────────────────────────────────┤
│ ▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯    ▯▯▯▯        │
└─────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────┐
│ ▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯    ▯▯▯▯        │
└─────────────────────────────────────────────────────┘
```

---

## レスポンシブ対応

### デスクトップ (≥ 1024px)
- 標準レイアウト
- パディング: 32px
- セクションヘッダー: フル表示

### タブレット (768px - 1023px)
- パディング: 24px
- セクションヘッダー: やや縮小

### モバイル (< 768px)
- パディング: 16px
- セクションヘッダー: 件数省略（"OVERDUE" のみ表示）
- タスクアイテム: コンパクト表示

---

## アクセシビリティ

### ARIAラベル

```html
<!-- Today ビュー -->
<div role="main" aria-labelledby="today-title">
  <h1 id="today-title">Today</h1>
  <p class="today-date" aria-label="Current date">Saturday, February 1</p>

  <!-- Overdue セクション -->
  <section aria-labelledby="overdue-header">
    <h2 id="overdue-header" class="section-header-title overdue">
      Overdue - 2 tasks
    </h2>
    <div role="list" aria-label="Overdue tasks">
      <!-- タスクアイテム -->
    </div>
  </section>

  <!-- Today セクション -->
  <section aria-labelledby="today-header">
    <h2 id="today-header" class="section-header-title today">
      Today - 3 tasks
    </h2>
    <div role="list" aria-label="Today's tasks">
      <!-- タスクアイテム -->
    </div>
  </section>
</div>
```

### スクリーンリーダー

- Overdue タスク: "Overdue task: プロジェクト提案書作成, Due January 30"
- Today タスク: "Task: 牛乳を買う, Due today"
- セクション変更: "Navigating to Overdue section" / "Navigating to Today section"

---

## 実装ノート

1. **日付計算**: クライアント側で現在日付を計算し、Overdue/Todayを判定
2. **自動更新**: 1分間隔でデータ再取得（日付変更も自動検出）
3. **セクション状態**: 折りたたみ状態をlocalStorageに保存（オプション）
4. **タイムゾーン**: ユーザーのローカルタイムゾーンを使用
5. **日付フォーマット**: ブラウザのロケールに基づいて日付を表示

**日付フォーマット例:**
```typescript
const formatDate = (date: Date): string => {
  return date.toLocaleDateString('ja-JP', {
    weekday: 'long',
    month: 'long',
    day: 'numeric'
  });
};
// 出力: "Saturday, February 1" または "土曜日, 2月1日"
```

---

**関連ドキュメント:**
- `docs/design_system.md` - デザインシステム全体
- `resources/mockups/dashboard.md` - ダッシュボード全体構造
- `resources/mockups/inbox.md` - Inbox画面
- `resources/mockups/task_create_form.md` - タスク作成フォーム
